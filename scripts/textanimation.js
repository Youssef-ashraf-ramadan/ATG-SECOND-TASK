// trig 1
new ScrollMagic.Scene({
  triggerElement: ".trig1",
})
  .addTo(controller)
  .on("enter", () => {
    gsap.from(".heading, .desc, .bottom, .skip", {
      duration: 1,
      y: 40,
      ease: "expo.out",
    });

    gsap.from(".title, .top, .caseStudy", {
      duration: 1,
      y: -40,
      ease: "expo.out",
    });
    
  });

new ScrollMagic.Scene({
  triggerElement: ".trig1",
})
  .addTo(controller)
  .on("leave", () => {
    gsap.from(".heading, .desc, .bottom, .skip", {
      duration: 1,
      y: -40,
      ease: "expo.out",
    });

    gsap.from(".title, .top, .caseStudy", {
      duration: 1,
      y: 40,
      ease: "expo.out",
    });
  });

// trig 2
new ScrollMagic.Scene({
  triggerElement: ".trig2",
})
  .addTo(controller)
  .on("enter", () => {
    gsap.from(".heading, .desc, .bottom, .skip", {
      duration: 1,
      y: -40,
      ease: "expo.out",
    });

    gsap.from(".title, .top, .caseStudy", {
      duration: 1,
      y: 40,
      ease: "expo.out",
    });
  });

new ScrollMagic.Scene({
  triggerElement: ".trig2",
})
  .addTo(controller)
  .on("leave", () => {
    gsap.from(".heading, .desc, .bottom, .skip", {
      duration: 1,
      y: 40,
      ease: "expo.out",
    });

    gsap.from(".title, .top, .caseStudy", {
      duration: 1,
      y: -40,
      ease: "expo.out",
    });
  });

// trig 3
new ScrollMagic.Scene({
  triggerElement: ".trig3",
})
  .addTo(controller)
  .on("enter", () => {
    gsap.from(".heading, .desc, .bottom, .skip", {
      duration: 1,
      y: 40,
      ease: "expo.out",
    });

    gsap.from(".title, .top, .caseStudy", {
      duration: 1,
      y: -40,
      ease: "expo.out",
    });
  });

new ScrollMagic.Scene({
  triggerElement: ".trig3",
})
  .addTo(controller)
  .on("leave", () => {
    gsap.from(".heading, .desc, .bottom, .skip", {
      duration: 1,
      y: -40,
      ease: "expo.out",
    });

    gsap.from(".title, .top, .caseStudy", {
      duration: 1,
      y: 40,
      ease: "expo.out",
    });
  });

// trig 4
new ScrollMagic.Scene({
  triggerElement: ".trig4",
})
  .addTo(controller)
  .on("enter", () => {
    gsap.from(".heading, .desc, .bottom, .skip", {
      duration: 1,
      y: -40,
      ease: "expo.out",
    });

    gsap.from(".title, .top, .caseStudy", {
      duration: 1,
      y: 40,
      ease: "expo.out",
    });
  });

new ScrollMagic.Scene({
  triggerElement: ".trig4",
})
  .addTo(controller)
  .on("leave", () => {
    gsap.from(".heading, .desc, .bottom, .skip", {
      duration: 1,
      y: 40,
      ease: "expo.out",
    });

    gsap.from(".title, .top, .caseStudy", {
      duration: 1,
      y: -40,
      ease: "expo.out",
    });
  });

// trig 5
new ScrollMagic.Scene({
  triggerElement: ".trig5",
})
  .addTo(controller)
  .on("enter", () => {
    gsap.from(".heading, .desc, .bottom, .skip", {
      duration: 1,
      y: 40,
      ease: "expo.out",
    });

    gsap.from(".title, .top, .caseStudy", {
      duration: 1,
      y: -40,
      ease: "expo.out",
    });
  });

new ScrollMagic.Scene({
  triggerElement: ".trig5",
})
  .addTo(controller)
  .on("leave", () => {
    gsap.from(".heading, .desc, .bottom, .skip", {
      duration: 1,
      y: -40,
      ease: "expo.out",
    });

    gsap.from(".title, .top, .caseStudy", {
      duration: 1,
      y: 40,
      ease: "expo.out",
    });
  });

// trig 6
new ScrollMagic.Scene({
  triggerElement: ".trig6",
})
  .addTo(controller)
  .on("enter", () => {
    gsap.from(".heading, .desc, .bottom, .skip", {
      duration: 1,
      y: -40,
      ease: "expo.out",
    });

    gsap.from(".title, .top, .caseStudy", {
      duration: 1,
      y: 40,
      ease: "expo.out",
    });
  });

new ScrollMagic.Scene({
  triggerElement: ".trig6",
})
  .addTo(controller)
  .on("leave", () => {
    gsap.from(".heading, .desc, .bottom, .skip", {
      duration: 1,
      y: 40,
      ease: "expo.out",
    });

    gsap.from(".title, .top, .caseStudy", {
      duration: 1,
      y: -40,
      ease: "expo.out",
    });
  });
