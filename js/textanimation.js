// trig 1
new ScrollMagic.Scene({
  triggerElement: ".trigger2",
})
  .addTo(controller)
  .on("enter", () => {
    gsap.from(".textheading, .footer-title, .bottom, .skip", {
      duration: 1,
      y: 40,
      ease: "expo.out",
    });

    gsap.from(".ABC, .Texttop, .Soon", {
      duration: 1,
      y: -40,
      ease: "expo.out",
    });
    
  });

new ScrollMagic.Scene({
  triggerElement: ".trigger2",
})
  .addTo(controller)
  .on("leave", () => {
    gsap.from(".textheading, .footer-title, .bottom, .skip", {
      duration: 1,
      y: -40,
      ease: "expo.out",
    });

    gsap.from(".ABC, .Texttop, .Soon", {
      duration: 1,
      y: 40,
      ease: "expo.out",
    });
  });

// trig 2
new ScrollMagic.Scene({
  triggerElement: ".trigger3",
})
  .addTo(controller)
  .on("enter", () => {
    gsap.from(".textheading, .footer-title, .bottom, .skip", {
      duration: 1,
      y: -40,
      ease: "expo.out",
    });

    gsap.from(".ABC, .Texttop, .Soon", {
      duration: 1,
      y: 40,
      ease: "expo.out",
    });
  });

new ScrollMagic.Scene({
  triggerElement: ".trigger3",
})
  .addTo(controller)
  .on("leave", () => {
    gsap.from(".textheading, .footer-title, .bottom, .skip", {
      duration: 1,
      y: 40,
      ease: "expo.out",
    });

    gsap.from(".ABC, .Texttop, .Soon", {
      duration: 1,
      y: -40,
      ease: "expo.out",
    });
  });

// trig 3
new ScrollMagic.Scene({
  triggerElement: ".trigger4",
})
  .addTo(controller)
  .on("enter", () => {
    gsap.from(".textheading, .footer-title, .bottom, .skip", {
      duration: 1,
      y: 40,
      ease: "expo.out",
    });

    gsap.from(".ABC, .Texttop, .Soon", {
      duration: 1,
      y: -40,
      ease: "expo.out",
    });
  });

new ScrollMagic.Scene({
  triggerElement: ".trigger4",
})
  .addTo(controller)
  .on("leave", () => {
    gsap.from(".textheading, .footer-title, .bottom, .skip", {
      duration: 1,
      y: -40,
      ease: "expo.out",
    });

    gsap.from(".ABC, .Texttop, .Soon", {
      duration: 1,
      y: 40,
      ease: "expo.out",
    });
  });

// trig 4
new ScrollMagic.Scene({
  triggerElement: ".trigger5",
})
  .addTo(controller)
  .on("enter", () => {
    gsap.from(".textheading, .footer-title, .bottom, .skip", {
      duration: 1,
      y: -40,
      ease: "expo.out",
    });

    gsap.from(".ABC, .Texttop, .Soon", {
      duration: 1,
      y: 40,
      ease: "expo.out",
    });
  });

new ScrollMagic.Scene({
  triggerElement: ".trigger5",
})
  .addTo(controller)
  .on("leave", () => {
    gsap.from(".textheading, .footer-title, .bottom, .skip", {
      duration: 1,
      y: 40,
      ease: "expo.out",
    });

    gsap.from(".ABC, .Texttop, .Soon", {
      duration: 1,
      y: -40,
      ease: "expo.out",
    });
  });

// trig 5
new ScrollMagic.Scene({
  triggerElement: ".trigger6",
})
  .addTo(controller)
  .on("enter", () => {
    gsap.from(".textheading, .footer-title, .bottom, .skip", {
      duration: 1,
      y: 40,
      ease: "expo.out",
    });

    gsap.from(".ABC, .Texttop, .Soon", {
      duration: 1,
      y: -40,
      ease: "expo.out",
    });
  });

new ScrollMagic.Scene({
  triggerElement: ".trigger6",
})
  .addTo(controller)
  .on("leave", () => {
    gsap.from(".textheading, .footer-title, .bottom, .skip", {
      duration: 1,
      y: -40,
      ease: "expo.out",
    });

    gsap.from(".ABC, .Texttop, .Soon", {
      duration: 1,
      y: 40,
      ease: "expo.out",
    });
  });

// trig 6
new ScrollMagic.Scene({
  triggerElement: ".trigger7",
})
  .addTo(controller)
  .on("enter", () => {
    gsap.from(".textheading, .footer-title, .bottom, .skip", {
      duration: 1,
      y: -40,
      ease: "expo.out",
    });

    gsap.from(".ABC, .Texttop, .Soon", {
      duration: 1,
      y: 40,
      ease: "expo.out",
    });
  });

new ScrollMagic.Scene({
  triggerElement: ".trigger7",
})
  .addTo(controller)
  .on("leave", () => {
    gsap.from(".textheading, .footer-title, .bottom, .skip", {
      duration: 1,
      y: 40,
      ease: "expo.out",
    });

    gsap.from(".ABC, .Texttop, .Soon", {
      duration: 1,
      y: -40,
      ease: "expo.out",
    });
  });
